---
"mycel": minor
---

Add experimental Codex subscription authentication while keeping tool execution inside Mycel. Enable `codex_subscription_auth` and configure a `storage = "codex"` provider to use it.
